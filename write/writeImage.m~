function writeImage(filename, img, cmap)
%WRITE AN IMAGE TO PNG
%
% Writes the image 'img' to a png-file wi 

if nargin < 3
    cmap = gray(256);
end

% img = (img + min(0,min(img(:))) ) / (max(img(:)) + min(0,min(img(:))) );

imwrite(255*img, cmap, [filename '.png']) 
    
end